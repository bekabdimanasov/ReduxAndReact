import phones from 'api/mockPhones'

export const fetchPhones = async () => {
  //return await axios.get('/api') (Реальном проекте)
  return new Promise((resolve, reject) => {
    resolve(phones)
    //reject('error')
  })
}